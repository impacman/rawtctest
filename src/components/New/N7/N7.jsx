import styled from "../New.module.scss";
import Video from "../../Video";

const N7 = ({ active }) => (
  <div className={styled.New}>
    <Video active={active} name={"new7"} />
  </div>
);

export default N7;
